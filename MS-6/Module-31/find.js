// find selects elements based on a condition and returns the first element which meet the condition otherwise returns undefined

const players = [75, 65, 67 , 74, 54]
const selected = players.find(p => p > 50)
console.log(selected)